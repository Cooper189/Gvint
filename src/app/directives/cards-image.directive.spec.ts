import { CardsImageDirective } from './cards-image.directive';

describe('CardsImageDirective', () => {
  it('should create an instance', () => {
    const directive = new CardsImageDirective();
    expect(directive).toBeTruthy();
  });
});
